## This configuration file is generated by https://docker.emqx.dev
## The following environment variables have been added to docker-compose.yml:
## EMQX_HOST=docker.emqx.dev
## EMQX_NODE__DATA_DIR="data"
## EMQX_DASHBOARD__LISTENERS__HTTP__BIND=18083
## EMQX_LOG__FILE__DEFAULT__ENABLE=true
## EMQX_LOG__CONSOLE__ENABLE=false

## EMQX_LISTENERS__SSL__DEFAULT=marked_for_deletion
## EMQX_LISTENERS__WS__DEFAULT=marked_for_deletion
## EMQX_LISTENERS__WSS__DEFAULT=marked_for_deletion

node {
  ## Do not modify the node name here, please configure EMQX_HOST and hostname in docker-compose.yaml
  ## name = "emqx@docker.emqx.dev"
  cookie = "docker-mqtt-broker-cookie"
}

cluster {
  name = emqxcl
  discovery_strategy = manual
}

## This docker-compose setup will disable console logging (docker logs) by default and enable file rotation logging

## To modify log settings other than configuration, directly change them in web Dashboard |> Management |> Logging

## Do not modify the file log path, docker mounts to fixed directory /opt/emqx/log/

